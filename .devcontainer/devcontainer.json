{
    "build": {
        "dockerfile": "Dockerfile",
        "context": "../"
    },
    "forwardPorts": [5000],
    "runArgs": [ "-v", "${localEnv:POSTGRES_CRED_LOCATION}/:/temp/", "-p", "25000:5000"]
}