{
    "build": {
        "dockerfile": "Dockerfile",
        "context": "../"
    },
    "forwardPorts": [5000],
    "runArgs": [ "-v", "${localEnv:POSTGRES_CRED_LOCATION}/:/temp/", "-p","25001:5000", "-v","${localEnv:AWS_CRED_LOCATION}/:/root/.aws"],
    "shutdownAction": "stopContainer"
}